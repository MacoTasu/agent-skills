# CONTRIBUTING

## 何を追加するか

追加するのは **「このパックに無かったせいで、レビューで問題を見逃しかけた観点」だけ**。

良い指摘を片端から書き足すとチェックリストは膨張し、エージェントが読む量が増えて
発火精度が下がる。取りこぼしの記録としてだけ育てる。

## 項目の書式

全項目を固定4フィールドで書く。

```markdown
## <観点のタイトル>

- **なぜ危険か**: 守らなかった時に実際に起きる事故。抽象的なリスクではなく、
  何がどう壊れるかを書く。
- **どう検出するか**: 見るファイル・grep・実行コマンド。エージェントがそのまま
  実行できる形で書く。
- **よくある間違った実装**: 実際に踏まれるパターン。
```

**「どう検出するか」が書けない項目は採用しない。** レビュー時に実行できない項目は
チェックリストではなくポエムで、ノイズにしかならない。

## PR チェック項目

- [ ] 特定の企業・プロダクトの固有名詞、内部の数値、固有のアーキテクチャを含んでいない
      （一般化した観点に落としてある）
- [ ] 追加した項目に「どう検出するか」が実行可能な形で書かれている

## バージョニング

**初版を公開するまでは `0.1.0` のまま据え置く。** 誰も参照していない間のバージョン更新は
何の情報も持たない。「公開した」= 各プラグインが `/plugin install` で導入可能になり、
かつ実際の PR で一度使われた状態を指す。

それ以降は `plugins/<name>/.claude-plugin/plugin.json` の `version` を semver で更新する。

- 観点・軸の追加 → minor
- 文言の修正・表現の改善 → patch
- スキル名やディレクトリ構造の変更 → major（利用者の参照が壊れるため）
