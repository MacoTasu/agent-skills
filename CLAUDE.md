# CLAUDE.md

**このリポジトリは public です。** ここに置いたものは、コミット履歴も含めて世界に公開されます。
以下は、そのことから導かれる原則。

---

## 1. 不用意にコミットを積まない

**push した時点で、履歴は公開物になります。** あとから消すのは想像より難しい。

- **こまめなコミットを積み上げない。** 意味のある単位にまとめてから commit する。
  試行錯誤の過程・一時的な誤り・途中で撤回した設計を、履歴として公開する必要はない
- **`git add -A` を安易に使わない。** 未追跡ファイルを巻き込む。パスを明示して add する
- コミットメッセージにも私的情報を書かない。**メッセージは diff と同じく公開物**

### 履歴を消すのが難しい理由（実際に踏んだこと）

squash して force push しても、**GitHub の API と Web UI からは旧コミットが閲覧可能なまま**でした。
`git fetch <sha>` は到達不能オブジェクトを配らないので消えたように見えますが、
`/repos/:owner/:repo/commits/:sha` は普通に返します。しかも **PR のタイムラインが旧 SHA を
全件列挙する**ので、推測する必要すらなく辿れます。

**結果として、リポジトリを削除して作り直す以外に確実な方法がありませんでした。**
stars も forks も付く前だったから取れた手で、少しでも広まっていたら選べません。

→ **後始末は高くつく。push する前に見る。**

## 2. 個人情報を載せない

- **コミット作者は noreply 形式を使う** — `<id>+<username>@users.noreply.github.com`。
  GitHub のプロフィールでメールを非公開にしていても、**コミットの author は別物**で、
  public リポジトリでは誰でも読めます
- 氏名・メールアドレス・連絡先・アカウント名（リポジトリ所有者として不可避なものを除く）

## 3. 開発マシンに関する情報を載せない

- **絶対パス** — `/Users/<name>/...` `/home/<name>/...` `~/` の展開結果
- **ホスト名・マシン名** — 自宅サーバやランナーの名前
- **ローカルのネットワーク情報** — プライベート IP、`localhost:<port>`、社内ドメイン

手順やスクリプトを書くときは、**実行する人の環境に依存しない形**にする。
`${CLAUDE_PLUGIN_ROOT}` や相対パス、あるいはプレースホルダを使う。

## 4. 機微情報を載せない

- API キー・トークン・秘密鍵・パスワード・認証情報
- **private リポジトリの名前とその内部構成**。名前を出すだけでも、その存在・言語・
  アーキテクチャ・事業領域が推測できてしまう
- 業務で得た知見は**一般化してから**入れる。特定企業の固有名詞・内部の数値・
  固有のアーキテクチャを含めない（[CONTRIBUTING.md](CONTRIBUTING.md) の PR チェックと同じ基準）

**例を書くときは架空か汎用にする。** 「例（Go の場合）」であって「例（<実在のプロジェクト> / Go）」ではない。

---

## push 前の確認

```bash
# 端末依存・個人情報・秘匿情報
git diff --cached | grep -nE '/Users/|/home/|@gmail|@[a-z-]+\.co\.jp|sk-ant|ghp_|gho_|AKIA|BEGIN [A-Z ]*PRIVATE KEY|192\.168\.|127\.0\.0\.1|localhost:[0-9]'

# private リポジトリ名など、自分が決めた NG ワード
git diff --cached | grep -niE '<自分のプロジェクト名>'

# 作者が noreply になっているか
git log --format='%ae' -1
```

**引っかかったら push しない。** commit を作り直すほうが、公開後に消すより桁違いに安い。
