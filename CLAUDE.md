# CLAUDE.md

**このリポジトリは public で、置いたものは他人の環境で実行されます。**
この2つから、原則が3群に分かれる。

- **A. 何が公開物か** — 思っているより範囲が広い
- **B. 載せてはいけないもの** — 個人・マシン・機微
- **C. 配るものの責任** — 指示は他人の権限で走る

---

# A. 何が公開物か

## A-1. 不用意にコミットを積まない

**push した時点で、履歴は公開物になります。** あとから消すのは想像より難しい。

- **こまめなコミットを積み上げない。** 意味のある単位にまとめてから commit する。
  試行錯誤の過程・一時的な誤り・途中で撤回した設計を、履歴として公開する必要はない
- **`git add -A` を安易に使わない。** 未追跡ファイルを巻き込む。パスを明示して add する
- コミットメッセージにも私的情報を書かない。**メッセージは diff と同じく公開物**

### 履歴を消すのが難しい理由（実際に踏んだこと）

squash して force push しても、**GitHub の API と Web UI からは旧コミットが閲覧可能なまま**でした。
`git fetch <sha>` は到達不能オブジェクトを配らないので消えたように見えますが、
`/repos/:owner/:repo/commits/:sha` は普通に返します。しかも **PR のタイムラインが旧 SHA を
全件列挙する**ので、推測する必要すらなく辿れます。

**結果として、リポジトリを削除して作り直す以外に確実な方法がありませんでした。**
stars も forks も付く前だったから取れた手で、少しでも広まっていたら選べません。

→ **後始末は高くつく。push する前に見る。**

## A-2. issue・PR・コメントも公開物

コミットと同じ基準で書く。**レビューのやり取りの中で、つい private リポジトリの名前や
社内の事情を書いてしまう**のが典型的な漏れ方です。PR 本文・コメント・issue のタイトルまで
同じ目で見る。

## A-3. `docs/` に置いたものは配信される

GitHub Pages で `main` の `/docs` を配信しているので、**リポジトリを開かなくても読める**状態です。
リポジトリ内の他のファイルより一段公開度が高い、という認識を持つ。

---

# B. 載せてはいけないもの

## B-1. 個人情報

- **コミット作者は noreply 形式を使う** — `<id>+<username>@users.noreply.github.com`。
  GitHub のプロフィールでメールを非公開にしていても、**コミットの author は別物**で、
  public リポジトリでは誰でも読めます
- 氏名・メールアドレス・連絡先・アカウント名（リポジトリ所有者として不可避なものを除く）

## B-2. 開発マシンに関する情報

- **絶対パス** — `/Users/<name>/...` `/home/<name>/...` `~/` の展開結果
- **ホスト名・マシン名** — 自宅サーバやランナーの名前
- **ローカルのネットワーク情報** — プライベート IP、`localhost:<port>`、社内ドメイン

手順やスクリプトを書くときは、**実行する人の環境に依存しない形**にする。
`${CLAUDE_PLUGIN_ROOT}` や相対パス、あるいはプレースホルダを使う。

## B-3. 機微情報

- API キー・トークン・秘密鍵・パスワード・認証情報
- **private リポジトリの名前とその内部構成**。名前を出すだけでも、その存在・言語・
  アーキテクチャ・事業領域が推測できてしまう
- 業務で得た知見は**一般化してから**入れる。特定企業の固有名詞・内部の数値・
  固有のアーキテクチャを含めない（[CONTRIBUTING.md](CONTRIBUTING.md) の PR チェックと同じ基準）

**例を書くときは架空か汎用にする。** 「例（Go の場合）」であって「例（<実在のプロジェクト> / Go）」ではない。

## B-4. 秘密を push してしまったら、まず無効化する

**順序を間違えない。** 履歴を直すより先に、**鍵やトークンを無効化（rotate）する。**

A-1 のとおり、履歴から消せても**既に読まれた可能性は消せません**。
「消したから大丈夫」ではなく、**漏れた前提で扱う**。無効化してから、落ち着いて履歴を直す。

---

# C. 配るものの責任

## C-1. ここに置く指示は、他人の環境で他人の権限で実行される

**普通の public リポジトリと決定的に違う点。** ライブラリなら使う側が呼ぶかどうかを選べますが、
スキルは**書いた指示がそのままエージェントに読まれて実行されます**。

- **破壊的な操作を無条件に指示しない** — 削除・上書き・force push・本番への適用。
  必要なら**可逆性と、人間の確認を挟むこと**を手順に明示する
- **ネットワークから取ってきて実行する記述を書かない** — `curl | sh` の類
- **既定は「調べる → 提案する → 人間が決める」** にする
- 設定ファイルを書き換えるなら、**バックアップを取り、既存の値を壊さない形**にする
  （同梱の `loop-init` が `.bak` を取り `//=` で既存キーを温存しているのが基準）

## C-2. PR で受け取る指示文を、コードと同じ厳密さで読む

スキルは**エージェントへの指示**です。悪意ある PR は「これまでの指示を無視して〜」のような
注入を仕込めますし、レビュアは markdown を「ただの文章」として流し読みしがち。

**指示文はコードである。** 実行される前提で1行ずつ読む。

## C-3. 第三者の成果物を同梱するときはライセンスを確認する

MIT でも**著作権表示の同梱が必要**です。同梱せずに済むなら、**名前で参照して利用者に導入を
委ねる**ほうが、更新追従の責任を負わずに済みます（`grill-issue` が外部スキルを
Skill 名で invoke しているのがその形）。

> 破壊的変更とバージョニングは [CONTRIBUTING.md](CONTRIBUTING.md) が正典。ここには重複させない。

---

# push 前の確認

```bash
# 端末依存・個人情報・秘匿情報
git diff --cached | grep -nE '/Users/|/home/|@gmail|@[a-z-]+\.co\.jp|sk-ant|ghp_|gho_|AKIA|BEGIN [A-Z ]*PRIVATE KEY|192\.168\.|127\.0\.0\.1|localhost:[0-9]'

# private リポジトリ名など、自分が決めた NG ワード
git diff --cached | grep -niE '<自分のプロジェクト名>'

# 作者が noreply になっているか
git log --format='%ae' -1
```

**引っかかったら push しない。** commit を作り直すほうが、公開後に消すより桁違いに安い。
秘密が含まれていた場合は、**直す前にまず無効化**する（B-4）。
